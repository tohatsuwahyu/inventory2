{
  "name": "inventory-app-jp-ui",
  "version": "1.0.0",
  "private": true,
  "description": "inventory web app",
  "scripts": {
    "serve": "npx http-server -p 3000 -c-1 .",
    "format": "npx prettier -w ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "http-server": "^14.1.1"
  }
}
